import type { NextPage } from "next";

const Chess: NextPage = () => {
  return (
    <main>
      <ChessBoard />
    </main>
  );
};

const ChessBoard = () => {
  return <></>;
};

export default Chess;
